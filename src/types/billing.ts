// 计费模式枚举
export enum BillingMode {
  PER_USE = 'per_use',        // 按次计费
  PER_ITEM = 'per_item',      // 按项目计费
  MONTHLY = 'monthly',        // 包月
  ONE_TIME = 'one_time',      // 一次性付费
  FREE = 'free'               // 免费
}
